#!/usr/bin/env sh
set -eu

repository_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)

echo "Python verification"

if ! find "$repository_root" \
  -path '*/.git' -prune -o \
  -path '*/node_modules' -prune -o \
  -name pyproject.toml -print -quit | grep -q .; then
  echo "  not configured: no Python project found"
  exit 0
fi

command -v uv >/dev/null 2>&1 || {
  echo "  failed: uv is required"
  exit 1
}

cd "$repository_root"
uv lock --check
uv sync --frozen
uv run ruff format --check src/KnowledgeApi src/ClaimSimulator
uv run ruff check src/KnowledgeApi src/ClaimSimulator
uv run pyright
uv run pytest
