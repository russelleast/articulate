#!/usr/bin/env sh
set -eu

repository_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
cd "$repository_root"

echo "Articulate repository verification"

for verifier in verify-node verify-python verify-dotnet verify-proto; do
  echo
  "${repository_root}/scripts/${verifier}"
done

echo
echo "Repository verification passed."
